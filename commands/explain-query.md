---
description: Paste a SQL query and get a plain-English explanation plus potential issues
argument-hint: <paste your SQL query>
allowed-tools: Read
---

The user wants a SQL query explained.

Their query: $ARGUMENTS

If $ARGUMENTS is empty, ask the user to paste the SQL query they want explained.

Read the skill file at `.claude/skills/query-explainer/SKILL.md` and follow its instructions exactly.
