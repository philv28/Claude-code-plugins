---
description: Paste a MySQL Workbench error and the SQL that caused it to get a diagnosis and fix
argument-hint: <paste the error message and your SQL>
allowed-tools: Read
---

The user has a MySQL error they need diagnosed.

Their input: $ARGUMENTS

If $ARGUMENTS is empty, ask the user to paste both the error message from MySQL Workbench and the SQL statement that caused it.

Read the skill file at `.claude/skills/sql-debugger/SKILL.md` and follow its instructions exactly.
